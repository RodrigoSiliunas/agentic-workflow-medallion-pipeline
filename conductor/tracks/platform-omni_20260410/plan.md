# Implementation Plan: Platform — Omni Multi-channel Integration

**Track ID:** platform-omni_20260410
**Spec:** [spec.md](./spec.md)
**Created:** 2026-04-10
**Status:** Complete

## Phase 1: Backend model + migration + schemas

### Tasks

- [x] Task 1.1: `app/models/channel.py` ganha classe `OmniInstance` (company_id, omni_instance_id, name, channel, state, last_sync_at, last_error)
- [x] Task 1.2: `app/models/__init__.py` reexporta `OmniInstance`
- [x] Task 1.3: Migration `b8e4c0d23f56_add_omni_instances` com 3 indices (omni_instance_id, state, company_id)
- [x] Task 1.4: `app/schemas/channel.py` com `CreateChannelRequest`, `ConnectChannelRequest`, `OmniInstanceResponse`, `QRCodeResponse` e literals `ChannelKind`/`OmniInstanceState`

## Phase 2: Backend router + pytest

### Tasks

- [x] Task 2.1: `app/api/routes/channels.py` com 5 endpoints: GET list, POST create, POST /{id}/connect, GET /{id}/qr, DELETE /{id}
- [x] Task 2.2: Fallback gracioso — se OmniService falha na criacao, marca state=failed e persiste last_error
- [x] Task 2.3: `app/main.py` registra router `/api/v1/channels`
- [x] Task 2.4: `tests/unit/test_channels_schemas.py` (6 tests)
- [x] Task 2.5: `tests/unit/test_omni_instance_model.py` (5 tests via SQLAlchemy inspect)
- [x] Task 2.6: Ruff 0 errors

## Phase 3: Frontend types + store + composable + pages

### Tasks

- [x] Task 3.1: `types/channel.ts` — OmniInstance, ChannelKind, OmniInstanceState, QRCodeInfo
- [x] Task 3.2: `composables/useChannelsApi.ts` — list/create/connect/disconnect/getQrCode com mappers snake→camel
- [x] Task 3.3: `stores/channels.ts` dual-source com 3 mocks (1 WhatsApp connected, 1 Discord connected, 1 Telegram connecting)
- [x] Task 3.4: `molecules/ChannelCard.vue` — card com icone, badge status, botoes action (pair/connect/resync/disconnect)
- [x] Task 3.5: `organisms/NewChannelModal.vue` — escolha canal + nome + validacao
- [x] Task 3.6: `organisms/QrPairingModal.vue` — polling 3s do QR + SVG mock pra dev offline + state success final
- [x] Task 3.7: `pages/channels/index.vue` — grid de ChannelCard + EmptyState + banner warning se Omni indisponivel + query param ?new=1

## Phase 4: Integracao + validacao

### Tasks

- [x] Task 4.1: `ModuleSwitcher.vue` adiciona tab "Canais" (icone phone) — agora 4 modulos
- [x] Task 4.2: `SidebarNav.vue` ganha mode `channels` com lista de instancias ativas + botao novo (navega com ?new=1) + dot colorido por estado
- [x] Task 4.3: `pages/settings.vue` — botao WhatsApp disabled vira link "Abrir Channels"
- [x] Task 4.4: Frontend: `nuxt prepare` limpo, `bun run lint` = 0 errors
- [x] Task 4.5: Backend: `ruff check` = All checks passed, `pytest tests/unit/` = 29 passed
- [x] Task 4.6: Fechar metadata + index + tracks.md

## Final Verification

- [x] 5 endpoints REST novos (`/api/v1/channels`) registrados
- [x] Fluxo end-to-end em mockMode: criar instancia → abrir QR modal → polling simulado → conectado
- [x] Integracao sidebar 4 modulos: Chat / Marketplace / Deploys / Canais
- [x] Track marcada como complete

## Follow-ups

- Discord/Telegram connect modal dedicado (hoje usa `window.prompt` — funcional mas cru)
- Polling automatico do estado via SSE quando Omni suportar
- Limite de 1 WhatsApp por empresa validado no backend (hoje e livre)
- UI para gerenciar `ChannelIdentity` (quais usuarios estao linkados a cada canal)

---

_Generated by Conductor._
