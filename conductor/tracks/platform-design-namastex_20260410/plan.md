# Implementation Plan: Platform — Design system Namastex.ai

**Track ID:** platform-design-namastex_20260410
**Spec:** [spec.md](./spec.md)
**Created:** 2026-04-10
**Status:** Complete

## Phase 1: Design tokens + Nuxt config

### Tasks

- [x] Task 1.1: `platform/frontend/DESIGN.md` reescrito — Cohere+namastex.ai, paleta brand 50→950, grayscale Namastex, Geist sans+mono, headings com tracking negativo, hero blur signature, status pulse keyframe, Agent Prompt Guide atualizado

- [x] Task 1.2: `platform/frontend/nuxt.config.ts` — `@nuxt/fonts` configurado com Geist (300/400/500/600/700) + Geist Mono (400/500) via provider Google. Adicionado `runtimeConfig.public.mockMode` (default true) para track B

- [x] Task 1.3: `platform/frontend/app/assets/css/main.css` — `@import "tailwindcss" + @import "@nuxt/ui"`. Bloco `@theme static` registra cor `namastex` (50→950) e `--font-sans/--font-mono`. Tokens CSS variables semanticos (`--bg`, `--surface`, `--text-*`, `--border-*`, `--status-*`, `--radius-*`, `--shadow-*`) com override `.dark`. Headings base com letter-spacing negativo. Hero blur + grid pattern utilities. Status pulse keyframes. Scrollbars custom

- [x] Task 1.4: `platform/frontend/app.config.ts` (novo) — `ui.colors.primary = "namastex"`, `ui.colors.neutral = "zinc"`, defaults de UButton (color primary, size md), defaults de UCard com radius e tokens

- [x] Task 1.5: `platform/frontend/app/app.vue` reescrito — root div com `bg-[var(--bg)]`, `text-[var(--text-primary)]`, `font-[var(--font-sans)]`. `useColorMode().preference = "dark"` no `onMounted` para forcar tema dark default. `useHead` com title e meta description Namastex

- [x] Task 1.6: Validacao local — `bun run postinstall` (= `nuxt prepare`) gerou `.nuxt` sem erros, apenas warnings inofensivos. `bun run lint` retorna **0 errors** apos fixes em arquivos legacy (any -> unknown, unused vars removidas em settings.vue, login.vue, useSettings.ts, useChat.ts, ActionCard.vue, SidebarNav.vue, auth.ts). 1 warning aceitavel (v-html em MessageBubble — planejado)

### Verification

- [x] DESIGN.md atualizado, paleta + Geist documentados
- [x] nuxt prepare passa sem errors
- [x] bun run lint: 0 errors
- [x] Tokens CSS exportados e prontos para Track B consumir
- [x] Dark mode default ativo no app.vue

## Final Verification

- [x] Acceptance criteria atendidos
- [x] Track B desbloqueada (pode consumir tokens via `var(--brand-*)`, `bg-[var(--surface)]`, etc)
- [x] Lint baseline em 0 errors para Track B nao quebrar CI

---

_Generated by Conductor._
