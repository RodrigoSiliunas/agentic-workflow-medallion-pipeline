# Specification: Infra Databricks + AWS + Bronze Layer

**Track ID:** infra-bronze_20260408
**Type:** Feature
**Created:** 2026-04-08
**Status:** Draft

## Summary

Setup completo do projeto (estrutura, dependencias, config) e implementacao da camada Bronze: ingestao de Parquet do S3, validacao de schema com evolution, e registro como Delta Table no Unity Catalog.

## Context

Primeira track do pipeline Medallion. Estabelece a fundacao sobre a qual Silver, Gold e o agente autonomo serao construidos. O design completo esta em `analise_arquitetural/analise.md`.

## User Story

Como avaliador do teste tecnico, quero ver um projeto bem estruturado com infraestrutura Databricks+AWS configurada e a camada Bronze funcional, para validar competencia em engenharia de dados e cloud.

## Acceptance Criteria

- [ ] `pyproject.toml` com dependencias, estrutura de diretorios conforme analise arquitetural
- [ ] `lib/schema/` com validacao de colunas obrigatorias + schema evolution (aceitar colunas novas)
- [ ] Testes unitarios para validacao de schema (`tests/test_schema/`)
- [ ] Notebook `bronze/ingest.py` que le Parquet do S3, valida schema, e salva como Delta Table
- [ ] Delta Table `bronze.conversations` registrada no Unity Catalog

## Dependencies

Nenhuma — primeira track, greenfield.

## Out of Scope

- Transformacoes Silver/Gold
- Agente autonomo (agent_pre, agent_post)
- Mascaramento de dados sensiveis
- Extratores de entidades (CPF, email, etc.)
- Notebooks de validacao (quality checks)

## Technical Notes

- Seguir estrutura de diretorios da `analise_arquitetural/analise.md`
- Parquet cru no S3 `/bronze/` separado da Delta Table `bronze.conversations` no Unity Catalog
- Schema validation: colunas obrigatorias devem estar presentes, colunas novas sao aceitas
- Delta Lake com `mergeSchema=true` para schema evolution
- Fingerprint via S3 metadata (name/size/modificationTime) para deteccao de mudancas

---

_Generated by Conductor. Review and edit as needed._
