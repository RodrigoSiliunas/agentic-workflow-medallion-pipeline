# Implementation Plan: Infra Databricks + AWS + Bronze Layer

**Track ID:** infra-bronze_20260408
**Spec:** [spec.md](./spec.md)
**Created:** 2026-04-08
**Status:** [x] Complete

## Overview

Estabelecer a fundacao do projeto: estrutura de diretorios, dependencias, lib de validacao de schema com testes, e notebook Bronze funcional no Databricks.

## Phase 1: Project Scaffolding

Setup da estrutura do projeto, dependencias, e configuracao de ferramentas.

### Tasks

- [x] Task 1.1: Criar `pyproject.toml` com dependencias (pydantic, pytest, ruff, pyspark dev)
- [x] Task 1.2: Criar estrutura de diretorios (`notebooks/`, `lib/`, `tests/`, `data/`)
- [x] Task 1.3: Configurar `ruff` no `pyproject.toml` (line-length=100, target py311)
- [x] Task 1.4: Criar `.env.example` com variaveis necessarias (MASKING_SECRET, S3_BUCKET, DATABRICKS_HOST)

### Verification

- [ ] `ruff check .` passa sem erros
- [ ] Estrutura de diretorios conforme analise arquitetural

## Phase 2: Schema Validation Library

Implementar `lib/schema/` com validacao de colunas obrigatorias e schema evolution. TDD: testes primeiro.

### Tasks

- [x] Task 2.1: Criar `tests/test_schema/test_validator.py` com casos de teste (colunas ok, coluna faltando, colunas extras, tipos)
- [x] Task 2.2: Implementar `pipeline_lib/schema/contracts.py` (REQUIRED_COLUMNS, VALUE_CONSTRAINTS)
- [x] Task 2.3: Implementar `pipeline_lib/schema/validator.py` (validate_schema, retorna ok/erro + warnings para colunas novas)
- [x] Task 2.4: Rodar testes e garantir que passam (8/8 GREEN)

### Verification

- [ ] `pytest tests/test_schema/ -v` passa com todos os testes verdes
- [ ] Validador aceita colunas extras sem erro
- [ ] Validador rejeita schema sem colunas obrigatorias

## Phase 3: Bronze Ingestion Notebook

Implementar notebook Databricks que le Parquet do S3, valida schema, e salva como Delta Table.

### Tasks

- [x] Task 3.1: Criar `notebooks/bronze/ingest.py` — leitura do Parquet do S3 via PySpark
- [x] Task 3.2: Integrar validacao de schema (importar pipeline_lib/schema) no notebook
- [x] Task 3.3: Salvar como Delta Table com `mergeSchema=true` e `saveAsTable("bronze.conversations")`
- [x] Task 3.4: Implementar fingerprint via S3 metadata para deteccao de mudancas
- [x] Task 3.5: Adicionar logging estruturado (linhas lidas, schema warnings, tempo de execucao)
- [x] Task 3.6: Setar `dbutils.jobs.taskValues` com status e metricas para tasks downstream

### Verification

- [ ] Notebook executa no Databricks sem erros
- [ ] Delta Table `bronze.conversations` existe no Unity Catalog
- [ ] `SELECT COUNT(*) FROM bronze.conversations` retorna 153228
- [ ] Schema da Delta Table contem as 14 colunas esperadas
- [ ] Re-execucao com mesmos dados e idempotente (nao duplica)

## Final Verification

- [ ] Todos os acceptance criteria da spec atendidos
- [ ] Testes passando (`pytest`)
- [ ] Lint passando (`ruff check .`)
- [ ] Estrutura de diretorios conforme analise arquitetural
- [ ] Commit feito com conventional commits

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
