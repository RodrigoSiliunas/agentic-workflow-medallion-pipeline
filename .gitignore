# Data files
data/
pipeline/data/
*.parquet

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
*.egg
.venv/
venv/
Lib/
# Windows venv dir (python -m venv .venv cria .venv/Scripts/).
# Não ignorar scripts/ no repo root (case-insensitive no Windows).
Scripts/
!/scripts/
pyvenv.cfg

# Environment
.env
.env.*
!.env.example

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Databricks
.databricks/

# Word docs (test instructions)
*.docx

# Frontend build artifacts
.nuxt/
.output/
node_modules/

# Platform backend venv
platform/backend/venv/

# Playwright MCP logs/screenshots
.playwright-mcp/

# E2E screenshots (testes locais, nao precisam estar no repo)
e2e-*.png

# Docs internos (gitignored).
docs/*
!docs/chaos_mode_via_databricks_ui.md
!docs/self_hosted_llm.md
conductor/

# Documentação pessoal não-técnica (Word/PPT/diagramas, fora do versionamento).
docs-pessoal/

# Design system repo (submodule/reference)
platform/design/

# Terraform
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.backup
*.tfplan
*.tfvars.local
terraform.tfvars.local

# Secrets
*.pem
*.key
mapping.encrypted
*.local.md
.rebuild-state.local.md

# Claude Code state (per-user)
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Caveman compress backup (auto-gerado pelo skill)
CLAUDE.original.md

# Pip leakage no scripts/ (instalação acidental no repo root no Windows)
scripts/pip*.exe
scripts/vba_extract.py
